node_modules/
.expo/
dist/
.env
*.log
.DS_Store

# Native projecten worden via prebuild/expo run gegenereerd (CNG) — niet committen
/android
/ios

# Mutatietesten: Stryker-sandbox + (regenereerbaar) rapport — niet committen
.stryker-tmp/
reports/

# EAS Submit-credentials: ASC API-key (.p8) + Google Play service-account JSON — geheim.
# Map + README blijven in de repo (documentatie); de sleutelbestanden zelf niet.
/credentials/*
!/credentials/README.md

# Firebase/FCM: google-services.json (per-project, build-time) + de FCM V1
# service-account-sleutel (geheim, voor de push-verzendkant bij EAS). Niet committen.
google-services.json
fcm-service-account*.json

# Android signing keystore(s) — geheim; downloaden via `eas credentials`, nooit committen.
*.jks
*.keystore
.env.eas-simulator
.claude/worktrees/
